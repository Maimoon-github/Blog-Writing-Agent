# Data

- `data/` holds reusable assets, cached research, and local resources.
- Store search result summaries, source metadata, and prompt templates here.
- Keep generated blog artifacts out of source control by placing them under `outputs/`.
