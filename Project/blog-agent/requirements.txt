# ================================================
# Autonomous AI Blog Agent - requirements.txt
# Pinned Feb 2026 • Python 3.10+ • Fully local stack
# Compatible with 8GB VRAM GPU (RTX 3060+) + CPU fallback
# ================================================

# ── Core LangGraph + LangChain (orchestrator-worker pattern)
langgraph==1.0.9
langchain-community==0.4.1
langgraph-checkpoint-sqlite==3.0.3

# ── UI
streamlit==1.54.0

# ── Vector store (Phase 2 RAG)
chromadb==1.5.1

# ── Image Generation (Stable Diffusion v1.4)
# IMPORTANT: Install Torch + CUDA FIRST (required for GPU acceleration)
# Run this BEFORE pip install -r requirements.txt:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
torch==2.10.0
torchvision==0.21.0
torchaudio==2.10.0
diffusers==0.36.0

# ── Tools & Utilities
beautifulsoup4==4.14.3
httpx==0.28.1
markdown==3.10.2
structlog==25.5.0

# ── Structured outputs (BlogPlan, Section, etc.)
pydantic==2.10.3